// propriedades são informações que podem ser verificadas

// propriedades são assim: variavel.propriedade

let nome = "João";

console.log(nome.length); // aqui procuramos quantos caracteres tem o nome

let numeros = [1, 23, 34, 5, 6];

console.log(numeros.length); // quantos números tem